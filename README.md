## 🚀 Quick Start

### Installation & Setup

```bash
# Clone the repository
git clone https://github.com/F96X-coder/CrashAnalyzer-pro.git

# Navigate to project directory
cd CrashAnalyzer-pro

# Install required dependencies
pip install -r requirements.txt

# Launch the application
python elegant_crash_analyzer.py                                                                                                                                                 # Update system first
pkg update && pkg upgrade  # Termux
# OR
sudo apt update && sudo apt upgrade  # Kali Linux

# Install Python and pip
pkg install python pip  # Termux
# OR
sudo apt install python3 python3-pip  # Kali Linux

# Then run the installation commands above
